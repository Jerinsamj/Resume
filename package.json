{
  "name": "resumecypress",
  "version": "1.0.0",
  "description": "Simple Cypress tests for Resume Website",
  "scripts": {
    "test": "cypress run",
    "report": "mochawesome-merge cypress/reports/*.json > cypress/reports/output.json && marge cypress/reports/output.json --reportDir public"
  },
  "dependencies": {
    "cypress": "^12.14.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}

